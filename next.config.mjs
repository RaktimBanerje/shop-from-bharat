/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Ensures the app runs in strict mode
  images: {
    domains: ['shopfrombharat-admin.apsgroup.in'],
  }
};

export default nextConfig;
